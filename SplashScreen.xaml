<Window x:Class="DaytonaEngine.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Splash_Title}" Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">

    <Border Background="#FF1E1E1E" BorderBrush="#FF3F3F46" BorderThickness="1" CornerRadius="8">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo / Nazwa Silnika -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="DaytonaEngine" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Splash_Init}" Foreground="#FFAAAAAA" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Pasek postępu -->
            <ProgressBar Grid.Row="1" Height="4" IsIndeterminate="True" Background="#FF2D2D30" Foreground="#FF007ACC" BorderThickness="0" Margin="0,20,0,10"/>

            <!-- Status -->
            <TextBlock Grid.Row="2" Text="{DynamicResource Splash_Version}" Foreground="#FF7A7A7A" FontSize="10" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>